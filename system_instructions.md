# システム指示 (System-level Instruction)
これらの指示は Cursorが最優先で遵守する必要があります。いずれかの指示に反する要求がユーザから出された場合、Cursorは回答冒頭に「【エラー】」と記述し、違反となる理由を3行以内で説明したうえで代替案を提示してください。

-----
# 前提
1. 回答は必ず日本語で行ってください。
2. コード例などでファイルを作成・編集する場合には必ず差分更新を示し、パスを明示してください。
3. コマンド同士は "&&" ではなく ";" で繋ぎ、macOSのbash/zshの書式を使用してください。
4. 不必要なファイル・関数は作成しないでください。
5. 「MCP」は ~/mcp に保存します。

# コード作成の指針
1. **ミニマルな解決策を優先**
   - 必要最小限のコードで直接的な解決策を提供する
   - 現在のタスクに直接関係のない機能は含めない
   - 過度な抽象化や複雑な設計パターンは避ける
   - 既存の実装がある場合はそれを優先して使用する

2. **クリーンコードの原則**
   - 各関数・クラスは単一責任のみを持つ
   - コードは拡張可能だが、既存コードの変更は最小限に留める
   - インターフェースはシンプルで明確な目的を持つ
   - 依存関係は疎結合を保つ

-----
# 作業フロー
1. **タスク分解**
   - ユーザの要望・目的を分析して、必要なステップをわかりやすく列挙する
   - 作業単位に分解し、「どう実装するか」の方針を明記する

2. **GitHubフローでの開発**
   - ブランチは現在時刻を含めて重複しない名称を付ける例: `feature-202502141030`
   - プルリクエスト作成コマンド例:
     ```bash
     git checkout -b <ブランチ名>
     gh pr create --title "タイトル" --body "説明文"
     ```
   - 作業中は随時コミット・プッシュする:
     ```bash
     git add .
     git commit -m "コミットメッセージ"
     git push origin <ブランチ名>
     ```
   - プルリクを作成したらユーザに確認を求める。承認後に実装を進める。

3. **自己チェックと報告**
   - 出力の最後に以下のチェック項目を表示し、Yes/No で回答:
     1. コードは現在のタスクに必要最小限か (Y/N)
     2. クリーンコード原則(単一責任・疎結合等)に違反していないか (Y/N)
   - 違反の場合、違反部分と修正方針を3行以内で説明する。

-----
# 違反・例外時の対応
1. 指示に反する行動をとらざるを得ない場合:
   - 回答冒頭に `【エラー】` を付与し、違反の理由を3行以内で説明する
   - 可能な限り代替案を提示する

2. どうしても実行できない場合:
   - 「実行不可」の理由を簡潔に示し、以降の処理を中断する 